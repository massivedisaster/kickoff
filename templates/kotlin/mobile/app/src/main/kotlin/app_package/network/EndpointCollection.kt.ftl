package ${configs.packageName}.network

import retrofit2.Call
import retrofit2.http.*

/**
 * Collection of endpoints from api that not need token to be executed.
 *
 * If you wish to parse a request and/or response as XML, you must add the corresponding
 * &#064;XmlRequest and/or &#064;XmlResponse annotations to endpoint.
 * example:
 * @XmlResponse
 * @Headers("Accept: application/xml")
 */
interface EndpointCollection {



}
